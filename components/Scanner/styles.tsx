import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  cameraContainer: {
    borderRadius: 20,
    overflow: "hidden",
    width: 305,
    height: 305,
  },
  camera: {
    flex: 1,
  },
});

export default styles;
